mlflow==2.16.0
scikit-learn==1.4.2
# numpy fijo para que el modelo serializado se pueda deserializar en la API
# (debe coincidir con api/requirements.txt; numpy 2.x no es compatible con 1.x
# al cargar el pickle). Antes era transitivo y podía derivar entre rebuilds.
numpy==2.4.2
pandas==2.2.2
boto3==1.34.0
psycopg2-binary==2.9.9
# Deps directas del pipeline (también vienen transitivas con mlflow; sin pin
# para no chocar con las versiones ya resueltas por mlflow).
requests
matplotlib
