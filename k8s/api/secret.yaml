# ⚠️ CREDENCIALES DE DESARROLLO — NO USAR EN PRODUCCIÓN.
# Valores ficticios para levantar el stack en local de forma reproducible. En
# producción gestiónalos FUERA de git (Sealed Secrets / SOPS / External Secrets
# o `kubectl create secret`) y rótalos. Ver README → "Seguridad y secretos".
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: mlops
type: Opaque
stringData:
  PG_DSN: postgresql://mlops_user:mlops_pass_2026@postgres-service.mlops.svc.cluster.local:5432/mlops
  AWS_ACCESS_KEY_ID: minioadmin
  AWS_SECRET_ACCESS_KEY: minioadmin123
  # Token del endpoint admin /reload-model (RF7). Cambiar en producción.
  RELOAD_TOKEN: reload-mlops-2026
