apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mlops

resources:
- configmap.yaml
- secrets.yaml
- deployment.yaml
- service.yaml

# El tag lo actualiza el CI (job bump-manifests) al SHA del commit construido.
images:
- name: dandiazc/mlops-pf-mlflow
  newName: dandiazc/mlops-pf-mlflow
  newTag: sha-d32e06dc717b5955072f3d292500d448d36120ab
