apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mlops
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

# El tag lo actualiza el CI (job bump-manifests) al SHA del commit construido.
images:
  - name: dandiazc/mlops-pf-ui
    newTag: latest
