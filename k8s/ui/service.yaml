apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: mlops
  labels:
    app: ui
spec:
  type: ClusterIP
  selector:
    app: ui
  ports:
    - name: http
      port: 8501
      targetPort: 8501
